version: 1
frontend:
  appRoot: login-proyecto/Frontend/frontend
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - CI=false npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*